# 쓰다듬기 수정 기록 — 2026-09-11

## 원인과 기존 구현 확인

마우스 이동은 `PetCanvas.mouseMoveEvent` → `PetWindow.hover` → `StrokeDetector.sample` 순서였다. Canvas에는 버튼 없는 이동을 받는 mouseTracking이 이미 켜져 있었다. 기본 창·드래그·좌표 변환을 새로 만들 필요는 없었다.

기존 코드를 실행하고 `QTest.mouseMove`로 화면의 이마 부근을 이동시켰다. `PetWindow.hover`를 직접 호출하지 않고 실제 Qt 이벤트 전달을 관찰했다. **버튼 없는 Canvas MouseMove 138개가 도착했지만 왕복 발동은 0회**였다. 기록한 왕복 구간에서 인접 이벤트 **18쌍의 시간값이 같았다**. 같은 기록을 기존 감지기에 다시 넣어도 발동은 0회였다.

이 환경의 Python 3.12.14 `time.get_clock_info('monotonic')`는 `GetTickCount64()`, 해상도 **0.015625초**를 보고한다. 기존 감지기는 `dt <= 0`이면 방향과 왕복 기록을 모두 지웠다. Windows에서 몰려 전달되는 이벤트가 그 조건을 반복해서 만족했다. 반면 이전 검증은 일정 시간 간격으로 `hover` 함수를 직접 호출했기 때문에 이 입력 경로 문제를 검출하지 못했다.

현재 머리 영역 `[-0.095, -0.025, 0.095, 0.07]`은 이마/앞머리 부근의 모델 공간 사각형이다. 이 모델의 model3에는 신뢰할 HitAreas가 등록되어 있지 않아 기존 프로필 영역을 유지했다. 마우스 위치는 로컬 논리 좌표에서 현재 MVP 역행렬을 통해 모델 공간으로 옮긴다. 확대·축소 시 캐시를 무효화하며 창 위치 이동은 로컬 좌표 변환에서 처리한다.

드래그는 좌클릭 즉시 시작하고 마우스를 캡처한다. 현재 자율 이동은 `Relocator`의 fade 재배치와 휴식 시각으로 관리한다. 수면은 Canvas.sleeping이 제어하고 native Sleep 모션을 재생한다. 깜빡임/시선은 앱에서 값으로 적용하고 native 물리는 기존 Update가 담당한다. 표정은 별도 LoadExtraExpression과 SetExpression/ResetExpressions를 사용한다.

## 변경

- 쓰다듬기 시간만 고해상도 `perf_counter()`로 측정한다. 이 PC에서는 QueryPerformanceCounter, 해상도 0.0000001초다. 기존 이동·수면의 시간 기준은 변경하지 않았다.
- 같은 시각/8ms 미만의 샘플은 유효한 기록을 지우지 않고 다음 샘플과 함께 처리한다.
- 방향 전환 시 순간적으로 느려지는 것을 취소로 처리하지 않는다. 유의미한 이동이 0.55초 이상 없을 때 종료한다.
- 거리·속도·노이즈·왕복 횟수·cooldown을 프로필에 모았다. 버튼 입력/드래그는 후보를 지우고, 놓은 뒤 0.4초에는 재발동할 수 없다.
- 실제 유효 이동 중 자율 이동을 멈추고, 머리 밖/정지 후 종료한다. 이미 시작한 반응은 유효 이동 중 유지하고 프로필의 3~4초 유지시간 뒤 0.65초에 걸쳐 복귀한다.
- 발동 기록 `PETTING detected=N`은 로그에만 남는다. 펫 화면에는 숫자나 영역을 표시하지 않는다.

## 조절값

모든 제스처 설정은 현재 `profiles/<id>.json`의 `behavior.petting`에 있다. 거리/속도는 화면 픽셀이 아닌 머리 영역 폭을 기준으로 한다.

| 설정 | 기본값 |
| --- | --- |
| 한 방향 최소 이동 | 머리 폭의 12% |
| 노이즈 무시 | 머리 폭의 1% 미만 |
| 누적 유효 이동 | 머리 폭의 65% 이상 |
| 방향 전환 | 3.2초 내 3회 |
| 최소/최대 속도 | 머리 폭의 0.08~8배/초 |
| 순간 점프 기준 | 머리 폭의 75% |
| 샘플 최소 간격 | 8ms |
| 무동작 종료 | 0.55초 |
| 재발동 cooldown | 2초 |
| 드래그 직후 차단 | 0.4초 |
| 반응 시작/유지/종료 | 0.35초 / 1초 / 0.65초 |

## Live2D 반응

실제 런타임에서 아래 7개가 존재하는 것을 확인했다. 없는 ID는 기존 방식대로 시작 시 필터링하고 최소/최대 범위로 제한한다.

- `ParamEyeLOpen/ROpen = 0.08`: 눈 감김 (런타임 범위 0~2).
- `ParamEyeLSmile/RSmile = 1`: 눈웃음 (0~1).
- `key2 = 0.8`: 기존 `02 脸红爱心` 표정에서 확인한 홍조/하트 (0~1).
- `ParamAngleZ = -5`, `ParamAngleY = 4`: 작은 고개 움직임 (-30~30).

기존 반응 레이어를 재사용했다. 반응 중 기존 표정 큐를 중지하고 자동 깜빡임/시선 다음에 절대값을 부드럽게 혼합한 뒤 native 물리·메시를 갱신한다. 매 프레임 기준값부터 계산하므로 누적되지 않는다. 반응 종료 시 이전 표정을 복구하고 수면이 시작되면 반응을 취소한다. 원본 ZIP/모델/워터마크는 수정하지 않았다.

## 실행 검증

```powershell
.\.venv\Scripts\python.exe -X utf8 -m unittest test_pet_behavior -v
.\.venv\Scripts\python.exe -u -X utf8 probe_petting.py
```

`probe_petting.py`는 별도 위치 파일을 사용하는 실제 펫 창을 띄운다. QTest로 마우스 포인터 이동과 버튼 press/release를 발생시키며, 이벤트 필터로 PetCanvas에 도착한 MouseMove/Enter/Leave를 기록한다. **사람의 손을 대신한 자동 입력 테스트이며 체감 검증이 아니다.**

수정 후 검사에서 단발 통과 0회, 정지 0회, 반복 왕복 1회 발동하고 반응 강도 1에 도달했다. 확장 검사에서는 MouseMove 504개가 전달됐고 다음 7개 항목이 통과했다:

1. 실제 이벤트 경로의 단발/정지 거부 및 왕복 수락.
2. 자동 눈 깜빡임·물리 업데이트 후 실제 반응 파라미터 값 일치.
3. 쓰다듬기 후 정지 시 반응 종료와 hover 재발동 없음.
4. 자율 이동 중 쓰다듬기 개입, 이전 표정 복구, 대기시간 재예약.
5. Qt 버튼 press/move/release 드래그 및 놓은 직후 쓰다듬기 오인 없음.
6. 수면 중 왕복 거부·이동 차단·깨우기.
7. 240px/400px 크기 및 창 위치 변경 후 같은 화면상의 머리 부근에서 발동.

결과: `artifacts/petting/probe.json`, `probe.png`, `reaction-240.png`, `reaction-400.png`. 단위 테스트 9개에는 동일 timestamp 이벤트 묶음, 방향 전환의 저속 구간, 노이즈/느린 움직임/빠른 단발 통과, cooldown, 드래그/정지 초기화를 포함한다. 기존 펫 회귀 검증은 `app.py --verify-pet full --state artifacts/petting/regression-state.json`으로 별도 실행한다.

기존 펫 회귀 검사 `--verify-pet full`도 29단계 모두 통과했다. 투명 프레임버퍼와 캐릭터 경계, 실제 Windows 입력 영역, 드래그·화면 경계 복구, 개발 제어판, 자율 이동 개입, 표정 복귀, 수면 전환, 저장을 포함한다. 이어 별도 프로세스의 `--verify-pet restore`에서 저장된 위치·크기 복원을 통과했다.

사람이 확인할 부분은 실제 손의 왕복 속도·거리 감도, 다양한 마우스 폴링 레이트, 물리적 다중 모니터/혼합 DPI에서의 체감이다. 이 항목들은 완료라고 판정하지 않았다. 무상호작용 표정 변화나 새로운 펫 기능은 추가하지 않았다.
